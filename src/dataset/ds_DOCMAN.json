{
	"name": "ds_DOCMAN",
	"properties": {
		"description": "Dataset description for tables in DOCMAN",
		"linkedServiceName": {
			"referenceName": "ls_DOCMAN",
			"type": "LinkedServiceReference",
			"parameters": {
				"ENVIRONMENT": {
					"value": "@dataset().ENVIRONMENT",
					"type": "Expression"
				},
				"SHORT_LOCATION": {
					"value": "@dataset().SHORT_LOCATION",
					"type": "Expression"
				},
				"DOCMAN": {
					"value": "@dataset().DOCMAN",
					"type": "Expression"
				},
				"DOCMAN_HOST": {
					"value": "@dataset().DOCMAN_HOST",
					"type": "Expression"
				},
				"DOCMAN_PORT": {
					"value": "@dataset().DOCMAN_PORT",
					"type": "Expression"
				},
				"DOCMAN_USERNAME": {
					"value": "@dataset().DOCMAN_USERNAME",
					"type": "Expression"
				},
				"LOGICAL_ENVIRONMENT": {
					"value": "@dataset().LOGICAL_ENVIRONMENT",
					"type": "Expression"
				}
			}
		},
		"parameters": {
			"SCHEMA": {
				"type": "string"
			},
			"TABLE": {
				"type": "string"
			},
			"ENVIRONMENT": {
				"type": "String",
				"defaultValue": "@pipeline().globalParameters.ENVIRONMENT"
			},
			"SHORT_LOCATION": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.SHORT_LOCATION"
			},
			"DOCMAN": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.DOCMAN"
			},
			"DOCMAN_HOST": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.DOCMAN_HOST"
			},
			"DOCMAN_PORT": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.DOCMAN_PORT"
			},
			"DOCMAN_USERNAME": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.DBDB_USERNAME"
			},
			"LOGICAL_ENVIRONMENT": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.LOGICAL_ENVIRONMENT"
			}
		},
		"annotations": [],
		"type": "OracleTable",
		"schema": [],
		"typeProperties": {
			"schema": {
				"value": "@dataset().SCHEMA",
				"type": "Expression"
			},
			"table": {
				"value": "@dataset().TABLE",
				"type": "Expression"
			}
		}
	}
}