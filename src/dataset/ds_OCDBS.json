{
	"name": "ds_OCDBS",
	"properties": {
		"description": "Connects to OCDBS",
		"linkedServiceName": {
			"referenceName": "ls_OCDBS",
			"type": "LinkedServiceReference",
			"parameters": {
				"ENVIRONMENT": {
					"value": "@dataset().ENVIRONMENT",
					"type": "Expression"
				},
				"LOGICAL_ENVIRONMENT": {
					"value": "@dataset().LOGICAL_ENVIRONMENT",
					"type": "Expression"
				},
				"SHORT_LOCATION": {
					"value": "@dataset().SHORT_LOCATION",
					"type": "Expression"
				},
				"OCDBS": {
					"value": "@dataset().OCDBS",
					"type": "Expression"
				},
				"OCDBS_HOST": {
					"value": "@dataset().OCDBS_HOST",
					"type": "Expression"
				},
				"OCDBS_PORT": {
					"value": "@dataset().OCDBS_PORT",
					"type": "Expression"
				},
				"OCDBS_USERNAME": {
					"value": "@dataset().OCDBS_USERNAME",
					"type": "Expression"
				},
				"OCDBS_SECRET_NAME": {
					"value": "@dataset().OCDBS_SECRET_NAME",
					"type": "Expression"
				}
			}
		},
		"parameters": {
			"ENVIRONMENT": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.ENVIRONMENT"
			},
			"SHORT_LOCATION": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.SHORT_LOCATION"
			},
			"LOGICAL_ENVIRONMENT": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.LOGICAL_ENVIRONMENT"
			},
			"OCDBS": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.OCDBS"
			},
			"OCDBS_HOST": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.OCDBS_HOST"
			},
			"OCDBS_PORT": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.OCDBS_PORT"
			},
			"OCDBS_USERNAME": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.OCDBS_USERNAME"
			},
			"OCDBS_SECRET_NAME": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.OCDBS_SECRET_NAME"
			},
			"SCHEMA": {
				"type": "string"
			},
			"TABLE": {
				"type": "string"
			}
		},
		"annotations": [],
		"type": "OracleTable",
		"schema": [],
		"typeProperties": {
			"schema": {
				"value": "@dataset().SCHEMA",
				"type": "Expression"
			},
			"table": {
				"value": "@dataset().TABLE",
				"type": "Expression"
			}
		}
	}
}