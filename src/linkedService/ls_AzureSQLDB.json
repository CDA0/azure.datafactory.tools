{
	"name": "ls_AzureSQLDB",
	"properties": {
		"parameters": {
			"ENVIRONMENT": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.ENVIRONMENT"
			},
			"SHORT_LOCATION": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.SHORT_LOCATION"
			},
			"LOGICAL_ENVIRONMENT": {
				"type": "string",
				"defaultValue": "@pipeline().globalParameters.LOGICAL_ENVIRONMENT"
			}
		},
		"annotations": [],
		"type": "AzureSqlDatabase",
		"typeProperties": {
			"connectionString": "Integrated Security=False;Encrypt=True;Connection Timeout=30;Data Source=sql-ct-@{linkedService().ENVIRONMENT}-@{linkedService().SHORT_LOCATION}-gupcom.database.windows.net;Initial Catalog=sdb-ct-@{linkedService().ENVIRONMENT}-@{linkedService().SHORT_LOCATION}-@{linkedService().LOGICAL_ENVIRONMENT}gup",
			"credential": {
				"referenceName": "cred_GUPCOM",
				"type": "CredentialReference"
			}
		},
		"connectVia": {
			"referenceName": "Azure",
			"type": "IntegrationRuntimeReference"
		}
	}
}